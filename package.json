{
  "name": "invoice-flow-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "echo 'NOTE: For Railway deployment, please configure two separate services with Root Directory set to /client and /server respectively.' && npm run build --workspaces",
    "start": "echo 'NOTE: Please start client and server separately.'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
